{
  "name": "@urlgen/edge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker — the redirect hot path",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@urlgen/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260801.1",
    "@urlgen/config": "workspace:*",
    "typescript": "5.9.3",
    "wrangler": "4.118.0"
  }
}
