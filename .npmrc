# Exact versions only — the project pins dependencies (see CLAUDE.md quality standards).
save-exact=true

# Surface phantom dependencies instead of hiding them behind a hoisted node_modules.
strict-peer-dependencies=false
