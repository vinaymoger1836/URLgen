{
  "name": "@urlgen/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Types, schemas and pure helpers shared by the edge worker, the API and the web app",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "@urlgen/config": "workspace:*",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
